import { ImpactOverlaysPipe } from './impact-overlays.pipe';

describe('ImpactOverlaysPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpactOverlaysPipe();
    expect(pipe).toBeTruthy();
  });
});
